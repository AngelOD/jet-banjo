using System;
using System.Collections.Generic;
using System.Text;

namespace JetBanjo.Utils.Data
{
    public class DataTypes
    {
        /// <summary>
        /// Enum for classification that associates Ranks with classes
        /// </summary>
        public enum Classification { A = 5, B = 4, C = 3, D = 2, E = 1 };

        /// <summary>
        /// Enum to represent seasons
        /// </summary>
        public enum Season { Summer, Winter }
    }
}
