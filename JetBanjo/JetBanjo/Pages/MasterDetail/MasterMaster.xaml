<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="JetBanjo.Pages.MasterDetail.MasterMaster"
             xmlns:i18n="clr-namespace:JetBanjo;assembly=JetBanjo"
             Title="Master"
             BackgroundColor="#3d81a8">
  <StackLayout WidthRequest="100">
    <ListView x:Name="MenuItemsListView"
              SeparatorVisibility="None"
              HasUnevenRows="true"
              ItemsSource="{Binding MenuItems}">
            <ListView.Header>
                <StackLayout Padding="0,25,0,15" HorizontalOptions="FillAndExpand" Spacing="25" >
                    <Label Text="{i18n:Translate app_name}" Font="26" HorizontalOptions="Center"/>
                </StackLayout>
            </ListView.Header>
      <ListView.ItemTemplate>
        <DataTemplate>
          <ViewCell>
            <StackLayout Padding="15,10" HorizontalOptions="FillAndExpand">
              <Label VerticalOptions="FillAndExpand" 
                    VerticalTextAlignment="Center" 
                    Text="{Binding Title}" 
                    FontSize="Large"
                    TextColor="White"/>
            </StackLayout>
          </ViewCell>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
  </StackLayout>
</ContentPage>